# 开发前确认报告模板

在写代码前，AI 必须先输出本报告。

## 1. 页面类型判断

- 当前页面类型：后台管理页面 / 非后台页面
- 是否属于第一版支持范围：是 / 否
- 判断依据：

## 2. Layout 边界判断

- Figma 中是否包含 Sidebar / Header / Menu：
- 项目中是否已有 Layout：
- 本次是否生成 Layout：
- 本次生成范围：

## 3. 项目上下文扫描结果

- 框架：
- UI 组件库：
- 样式方案：
- reference.tsx className 框架或规则：
- 是否允许沿用 reference.tsx 的 className 框架：
- 全局样式目录：
- 已有变量文件：
- 已有 Table 组件：
- 已有 Layout 组件：
- 已有 Tooltip / Tag / Card 等组件：

## 4. 输入材料分流判断

- 输入类型：单页面单尺寸 / 单页面多尺寸 / 同尺寸多状态 / 多页面多尺寸
- 判断依据：pageName / frameName / nodeId / 目录名 / 截图命名 / `meta.json` source
- 是否启用多尺寸基准适配流程：是 / 否
- 单尺寸时响应式策略来源：Figma 明确尺寸稿 / AI 按业务模块推导
- 多页面时是否已按业务页面分组：是 / 否 / 不适用
- `metadata.xml` 是否存在并纳入参考：是 / 否 / 不适用
- 待确认项：

## 5. 多尺寸源数据扫描表

| 目录 / 文件 | pageName / frameName | nodeId | 截图尺寸 | meta frame 尺寸 | 是否有 Sidebar / Menu / Header | Sidebar 宽度 | content 宽度 | content inner width | 是否状态变体 | 输入完整度 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |  |  |  |

如果文件名无法识别尺寸，应从 `meta.json` 的 frame width / height、截图尺寸、目录名中推断；仍无法确认时列入待确认项。

## 6. 基准稿 / 参考稿 / 状态变体归类

| 归类 | 尺寸 / 状态 | 节点 / 文件 | 用途 | 备注 |
| --- | --- | --- | --- | --- |
| 基准稿 | 最大宽度默认有数据态 |  | 默认结构与样式基准 |  |
| 响应式参考 |  |  | 模块级断点差异 |  |
| 状态变体 | empty / loading / error |  | 状态 UI | 不作为响应式断点 |

- 最大宽度是否为默认有数据态：是 / 否
- 如果最大宽度只有 empty / loading / error，是否已列为待确认：是 / 否 / 不适用
- 多尺寸中的样例数据差异是否已排除出响应式规则：是 / 否 / 不适用

## 7. Sidebar / Menu / Header 响应式归属确认

Sidebar / Menu / Header 属于全局 Layout 策略。项目已有 Layout 时，默认跟随项目现状，content 区域根据真实可用宽度适配。

| 尺寸 | 是否有 Sidebar / Menu / Header | Sidebar 宽度 | Figma 差异 | 处理归属 | 待用户确认 |
| --- | --- | --- | --- | --- | --- |
| 1440 |  |  |  | 全局 Layout / 本次页面 / 不适用 |  |
| 1024 |  |  |  | 全局 Layout / 本次页面 / 不适用 |  |
| 768 |  |  |  | 全局 Layout / 本次页面 / 不适用 |  |
| 520 |  |  | 工程验收断点，按相邻尺寸插值推导 | 全局 Layout / 本次页面 / 不适用 |  |
| 375 |  |  |  | 全局 Layout / 本次页面 / 不适用 |  |

- 768 及以下菜单是否隐藏：
- 隐藏后是否为抽屉 / 顶部入口 / 完全不展示：
- 页面适配基于 viewport 还是 content 可用宽度：

## 8. 颜色与设计 token 映射

颜色类样式以 Figma 实际色值为最高依据；只有项目已有变量、design token、`custom-figma-constant.less` 的色值与 Figma 实际色值完全一致时才复用。没有相同色值时，少量局部颜色可直接写在当前 class；重复出现或页面级核心颜色再沉淀到 `custom-figma-constant.less`，新增颜色变量必须使用 `@figma-` 前缀。

| 类型 | Figma 实际值 | 来源 | 相同色值变量 | 处理方式 | 变量名 / 直接写入 class |
| --- | --- | --- | --- | --- | --- |
| 字体颜色 |  |  |  |  |  |
| 背景色 |  |  |  |  |  |
| 边框色 |  |  |  |  |  |
| 图标色 |  |  |  |  |  |
| 阴影色 |  |  |  |  |  |
| 渐变色 |  |  |  |  |  |
| 状态色 |  |  |  |  |  |
| 字号 |  |  |  |  |  |
| 间距 |  |  |  |  |  |
| 圆角 |  |  |  |  |  |
| 阴影 |  |  |  |  |  |

## 9. className 解析方案

| 来源 | className 示例 | 框架 / 规则判断 | 解析出的 CSS 声明 | 与 Figma 实际数据是否一致 | 目标项目输出方式 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

## 10. Icon 尺寸识别方案

| 图标 | 资源节点 | 语义图标容器 | 容器尺寸 class / w-h | 内部 Vector inset | 最终 icon 尺寸 | Arco 写法 / 目标写法 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

## 11. 资源归一化方案

| 资源 | 多尺寸临时 URL 是否不同 | 生产资源处理 | 尺寸 / 位置 / 显隐适配 |
| --- | --- | --- | --- |
|  |  | 本地资源 / 项目已有资源 / 图标库 |  |

## 12. 组件拆分方案

| 模块 | 组件名 | 职责 | 是否复用 | 是否数据驱动 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 13. ViewModel 草案

```ts
// 在这里写类型草案
```

## 14. 语义化事件设计

| 组件 | 事件 | 参数 | 页面层处理 |
| --- | --- | --- | --- |
|  |  |  |  |

## 15. 响应式策略表

响应式优先级：用户明确说明优先；全局 Layout 策略跟随项目现状；content 区域优先参考多尺寸 Figma；单尺寸时才使用通用响应式推导规则。

| 模块 | 1440 | 1024 | 768 | 520 | 375 | 策略来源 | 溢出策略 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  | 按 768 / 375 插值推导，不与两侧冲突 |  | Figma 明确 / AI 推导 / 用户确认 |  |

## 16. 风险与待确认

- 
- 
- 

## 17. 是否可以开始编码

- 是 / 否
- 需要用户确认的问题：
